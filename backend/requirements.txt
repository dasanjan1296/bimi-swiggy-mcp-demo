fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
# Optional dev driver. Lets reviewers run the API without a local Postgres
# by setting DATABASE_URL=sqlite+aiosqlite:///./bimi.db (see .env.example).
aiosqlite>=0.20.0
alembic>=1.14.0
pydantic[email]>=2.10.0
pydantic-settings>=2.7.0
httpx>=0.28.0
python-multipart>=0.0.18
apscheduler>=3.11.0
firebase-admin>=6.6.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cryptography>=44.0.0
# Production observability — env-gated init (no-op when SENTRY_DSN unset).
sentry-sdk[fastapi]>=2.20.0

# Test + lint
pytest>=9.0.0
pytest-asyncio>=1.0.0
pytest-cov>=7.0.0
ruff>=0.15.0
